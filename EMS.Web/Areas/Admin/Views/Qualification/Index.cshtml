@model IEnumerable<EMS.Entities.Qualification>

@{
    ViewData["Title"] = "Qualification Management";
}

<h1>@ViewData["Title"]</h1>

<hr />
<div class="validation-message">
    @if (TempData["ErrorMessage"] != null)
    {
        <p class="alert-danger">@TempData["ErrorMessage"]</p>
    }
    @if (TempData["SuccessMessage"] != null)
    {
        <p class="alert-success">@TempData["SuccessMessage"]</p>
    }
</div>

<p>
    <a class="btn btn-primary" asp-action="Create">Create New</a>
</p>
<table class="datatable table table-striped table-bordered table-hover">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.QualificationName)
            </th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.QualificationName)
                </td>
                <td>
                    <form asp-action="Delete" asp-route-id="@item.QualificationId" method="post">
                        <a asp-action="Edit" asp-route-id="@item.QualificationId"><img src="~/Images/Icons/edit.png" title="Edit" class="icon" /></a>
                        <button type="submit" onclick="return confirm('Are you sure you want to delete Qualification :@item.QualificationName ?')"><img src="~/Images/Icons/delete.png" title="Delete" class="icon" /></button>
                    </form>
                </td>
            </tr>
        }
    </tbody>
</table>
